# نسخة الأمان — الإصدار 9.8 المستقر

أُرشفت قبل البدء بالتحديث الشامل للمعمار.

## للعودة إليها بخطوة واحدة
انسخ محتوى `backup/v9.8/index.html` إلى `index.html` بجذر المستودع، فتعود النسخة الحية كما كانت.
أو استخدم الوسم `v9.8-stable` لاسترجاع كل الملفات بحالتها.

## ملاحظة مهمة
قاعدة البيانات (`data.json` و`ver.json`) مستقلة تماماً عن نسخة البرنامج، فلا تتأثر بأي رجوع أو تقدم بالإصدارات.
